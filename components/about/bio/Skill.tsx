import { ThemeIcon, Card } from "@components/shared";

export function Skill() {
  return (
    <Card className=" flex items-center gap-2 rounded-md col-span-full lg:col-span-1 lg:last-of-type:col-span-full">
      <ThemeIcon>Hello</ThemeIcon>
      <p>Adobe photoshop</p>
    </Card>
  );
}
